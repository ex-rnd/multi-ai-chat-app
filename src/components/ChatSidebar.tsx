import React from 'react';
import SidebarContent from './SidebarContent';

const ChatSidebar = () => {
  return (

      <div className="hidden lg:flex lg:w-80 lg:flex-col lg:border-r h-screen">
        <SidebarContent />
      </div>

  )
}

export default ChatSidebar;
